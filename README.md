# 🎬 Netflix Stock Dashboard

A Streamlit-based dashboard to visualize and analyze Netflix (NFLX) stock trends using Pandas and Plotly.

## 🚀 Features
* Interactive price charts (Plotly)
* Moving average calculations
* Data filtering by date range

## 🛠 Installation
1. Create environment: `python3 -m venv .venv`
2. Activate: `source .venv/bin/activate`
3. Install deps: `pip install -r requirements.txt`

## 📈 Usage
run the app with:
`streamlit run app.py`# Netflix-stock-dashboard
